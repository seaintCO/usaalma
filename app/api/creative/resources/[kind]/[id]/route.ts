import { NextResponse } from "next/server";
import { requirePaidUser } from "@/lib/api/requirePaidUser";
import { createClient } from "@/lib/supabase/server";
const tables:Record<string,string>={"brand-kits":"creative_brand_kits",campaigns:"creative_campaigns",folders:"creative_folders"};
export async function GET(_r:Request,c:{params:Promise<{kind:string;id:string}>}){const{user,error}=await requirePaidUser();if(error)return error;const p=await c.params,table=tables[p.kind];if(!table)return NextResponse.json({error:"Not found"},{status:404});const s=await createClient();const{data}=await s.from(table).select("*").eq("id",p.id).eq("user_id",user.id).maybeSingle();return data?NextResponse.json({item:data}):NextResponse.json({error:"Not found"},{status:404});}
export async function PATCH(r:Request,c:{params:Promise<{kind:string;id:string}>}){const{user,error}=await requirePaidUser();if(error)return error;const p=await c.params,table=tables[p.kind];if(!table)return NextResponse.json({error:"Not found"},{status:404});const body=await r.json(),s=await createClient();const{data,error:dbError}=await s.from(table).update(body).eq("id",p.id).eq("user_id",user.id).select().single();return dbError?NextResponse.json({error:"Could not update resource."},{status:400}):NextResponse.json({item:data});}
export async function DELETE(_r:Request,c:{params:Promise<{kind:string;id:string}>}){const{user,error}=await requirePaidUser();if(error)return error;const p=await c.params,table=tables[p.kind];if(!table)return NextResponse.json({error:"Not found"},{status:404});const s=await createClient();const{error:dbError}=await s.from(table).delete().eq("id",p.id).eq("user_id",user.id);return dbError?NextResponse.json({error:"Could not delete resource."},{status:400}):NextResponse.json({success:true});}
