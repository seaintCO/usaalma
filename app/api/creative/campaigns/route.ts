import OpenAI from "openai"; import { NextResponse } from "next/server"; import { requirePaidUser } from "@/lib/api/requirePaidUser"; import { createClient } from "@/lib/supabase/server";
export async function GET(){const{user,error}=await requirePaidUser();if(error)return error;const s=await createClient();const{data, error:dbError}=await s.from("creative_campaigns").select("*").eq("user_id",user.id).order("updated_at",{ascending:false});return dbError?NextResponse.json({error:"Campaigns could not be loaded."},{status:400}):NextResponse.json({campaigns:data??[]});}
export async function POST(request:Request){const{user,error}=await requirePaidUser();if(error)return error;const body=await request.json();if(!String(body.name??"").trim())return NextResponse.json({error:"Campaign name is required."},{status:400});let copy={socialCaptions:body.socialCaptions??"",adCopy:body.adCopy??"",productPhotoPrompt:body.productPhotoPrompt??""};if(body.generateCopy&&process.env.OPENAI_API_KEY){try{const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const response:any=await client.responses.create({model:(await import("@/lib/ai/models")).OPENAI_MODELS.fast,input:`Write concise campaign copy. Brand: ${body.brandName??""}. Concept: ${body.concept??""}. Audience: ${body.audience??""}. Return sections SOCIAL, AD, PHOTO.`});const text=response.output_text??"";copy={socialCaptions:text,adCopy:text,productPhotoPrompt:text};}catch{}}const s=await createClient();const{data, error:dbError}=await s.from("creative_campaigns").insert({user_id:user.id,brand_kit_id:body.brandKitId??null,folder_id:body.folderId??null,name:String(body.name).trim(),concept:body.concept??null,audience:body.audience??null,social_captions:copy.socialCaptions,ad_copy:copy.adCopy,product_photo_prompt:copy.productPhotoPrompt,metadata:body.metadata??{}}).select().single();return dbError?NextResponse.json({error:"Campaign could not be saved."},{status:400}):NextResponse.json({campaign:data},{status:201});}
