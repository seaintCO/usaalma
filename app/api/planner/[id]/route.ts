import { NextResponse } from "next/server";import { getCurrentUser } from "@/lib/auth/user";import { PlannerRepository } from "@/lib/db/repositories/planner/planner.repository";
export async function PATCH(r:Request,c:{params:Promise<{id:string}>}){const u=await getCurrentUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json(await PlannerRepository.update(u.id,(await c.params).id,await r.json()))}export async function DELETE(_r:Request,c:{params:Promise<{id:string}>}){const u=await getCurrentUser();if(!u)return NextResponse.json({error:"Unauthorized"},{status:401});await PlannerRepository.delete(u.id,(await c.params).id);return NextResponse.json({success:true})}
